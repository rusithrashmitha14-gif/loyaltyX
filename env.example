# ===========================================
# DATABASE (POSTGRESQL/SUPABASE)
# ===========================================
# Use a pooled connection string for Vercel (recommended):
#   postgresql://<user>:<password>@db.<ref>.supabase.co:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require
#
# Also set DIRECT_URL for running Prisma migrations (direct port 5432):
#   postgresql://<user>:<password>@db.<ref>.supabase.co:5432/postgres?sslmode=require
#
# Production (Supabase)
DATABASE_URL="postgresql://USER:PASSWORD@db.PROJECT_REF.supabase.co:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres?sslmode=require"

# Local development (if using local PostgreSQL)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/loyaltyx"
# DIRECT_URL="postgresql://postgres:password@localhost:5432/loyaltyx"

# ===========================================
# NEXTAUTH
# ===========================================
# Local: http://localhost:3000
# Staging: https://loyaltyx-staging.vercel.app
# Production: https://loyaltyx.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"

# ===========================================
# AUTHENTICATION & SECURITY
# ===========================================
# JWT secret for API authentication
# Generate with: openssl rand -base64 32
# IMPORTANT: Rotate this monthly in production
AUTH_SECRET="your_jwt_secret_key_here"
JWT_SECRET="your_long_random_secret_key"

# ===========================================
# APPLICATION
# ===========================================
NODE_ENV="development"

# Public API URL for client-side calls
# Local: http://localhost:3000/api
# Production: https://loyaltyx.vercel.app/api
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# ===========================================
# MONITORING & LOGGING (OPTIONAL)
# ===========================================
# Sentry DSN for error tracking
# Get from: https://sentry.io
# SENTRY_DSN="your_sentry_dsn_here"

# Vercel Analytics (automatically enabled on Vercel)
# No configuration needed

# ===========================================
# RATE LIMITING (OPTIONAL)
# ===========================================
# Upstash Redis for rate limiting
# Get from: https://upstash.com
# UPSTASH_REDIS_REST_URL=""
# UPSTASH_REDIS_REST_TOKEN=""
