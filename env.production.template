# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES TEMPLATE
# ===========================================
# Use this template when setting up Vercel Environment Variables
# DO NOT commit files with real secrets to git!

# ===========================================
# DATABASE (FROM SUPABASE)
# ===========================================
# Get this from Supabase → Project Settings → Database → Connection string
# Use the pooled connection string for Vercel (pgbouncer on port 6543):
# Format: postgresql://[user]:[password]@db.[project-ref].supabase.co:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require
DATABASE_URL="postgresql://postgres:password@db.project-ref.supabase.co:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"

# DIRECT_URL is required for Prisma migrations (non-pooled, port 5432)
# Format: postgresql://[user]:[password]@db.[project-ref].supabase.co:5432/postgres?sslmode=require
DIRECT_URL="postgresql://postgres:password@db.project-ref.supabase.co:5432/postgres?sslmode=require"

# ===========================================
# NEXTAUTH
# ===========================================
# Your Vercel production URL (update after first deployment)
NEXTAUTH_URL="https://your-project-name.vercel.app"

# Generate with: openssl rand -base64 32
# Keep this secret and rotate monthly!
NEXTAUTH_SECRET="GENERATE_RANDOM_32_CHAR_SECRET_HERE"

# ===========================================
# AUTHENTICATION & SECURITY
# ===========================================
# Generate with: openssl rand -base64 32
# These should be DIFFERENT from local dev secrets
AUTH_SECRET="GENERATE_RANDOM_32_CHAR_SECRET_HERE"
JWT_SECRET="GENERATE_RANDOM_32_CHAR_SECRET_HERE"

# ===========================================
# APPLICATION
# ===========================================
NODE_ENV="production"

# Your production API endpoint (update after first deployment)
NEXT_PUBLIC_API_URL="https://your-project-name.vercel.app/api"

# ===========================================
# OPTIONAL: ERROR MONITORING
# ===========================================
# Sentry DSN (if using Sentry for error tracking)
# SENTRY_DSN="https://xxxxxxxx@sentry.io/xxxxxxx"

# ===========================================
# OPTIONAL: RATE LIMITING
# ===========================================
# Upstash Redis (recommended for production rate limiting)
# Get from: https://console.upstash.com
# UPSTASH_REDIS_REST_URL="https://xxxxx.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="AXXxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"


